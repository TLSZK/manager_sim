server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This line ensures React Router handles the routing
        try_files $uri $uri/ /index.html;
    }
}